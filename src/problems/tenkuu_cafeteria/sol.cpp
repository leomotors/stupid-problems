#include <bits/stdc++.h>
using namespace std;

int C, O;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> C >> O;

    for (int i = 0; i < C; i++) {
        int c;
        cin >> c;
    }

    for (int i = 0; i < O; i++) {
        int v, u, w;
        cin >> v >> u >> w;
    }
}
