#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int Q;
    cin >> Q;

    for (int i = 0; i < Q; i++) {
        long long a, b;
        cin >> a >> b;
        cout << a + b << "\n";
    }
}
